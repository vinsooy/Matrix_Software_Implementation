# Environment Configuration Template
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=3000

# Authentication Tokens (comma-separated list of valid tokens)
VALID_TOKENS=your-token-here,another-token-here

# Security Settings
NODE_ENV=development

# Add other sensitive values here (API keys, database credentials, etc.)
# Never commit actual secrets to version control